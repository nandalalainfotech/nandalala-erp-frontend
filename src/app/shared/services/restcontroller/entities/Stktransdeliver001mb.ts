import { BaseEntity } from "./BaseEntity";

export class Stktransdeliver001mb extends BaseEntity {
    stdelId?: number;
    title?: string;
    status?: string;
    grandTotal?: number;
    delName?: string;
}