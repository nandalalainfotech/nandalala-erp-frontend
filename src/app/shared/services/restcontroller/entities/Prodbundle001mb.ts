import { BaseEntity } from "./BaseEntity";

export class Prodbundle001mb extends BaseEntity {
    pbundId?: number;
    itemCode?: string;
    childItem?: string;
    quantity?: string;
    description?: string;
}